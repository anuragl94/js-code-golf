const sort_=(w)=>w.split("").sort().join();
const check=(w)=>{
    for(var l of w.toUpperCase())
        if (l.charCodeAt()%65>25)return false;
    return true;
}
const solution=(a, b)=> (check(a) && check(b))?sort_(a)==sort_(b):false;